{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "dev:with-vuex-query": "lerna run dev --scope with-vuex-query --stream"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
